import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    position: 'absolute',
    bottom: '4%',
    left: '4%',
    right: '4%',
    zIndex: 1,
    elevation: 1,
    borderRadius: 4,
  },

  text: {
    padding: 14,
    color: 'white',
    fontSize: 16,
    textAlign: 'center',
  },
});
